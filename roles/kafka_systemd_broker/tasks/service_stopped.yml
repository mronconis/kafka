---
- name: Stop Kafka
  ansible.builtin.service:
    name: "{{ kafka_service_name }}"
    state: stopped
  become: true

- name: Ensure Kafka is stopped
  ansible.builtin.shell: jcmd | grep -i kafka
  register: kafka_check
  changed_when: false
  failed_when: kafka_check.rc == 0